<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link href="/main.css" rel="stylesheet" />
  </head>

  <body class="grey-bg">
    <%- include('nav.ejs') %>
    <div class="detail-bg">
      <h4><%= result.title %></h4>
      <p><%= result.content %></p>
      <p><%= result.user %></p>
      <a href="/chat/request?writerId=<%= result.user %>">채팅하기</a>
      <hr style="margin-top: 60px" />
      <div>
        <% if (result2 && result2.length > 0) { %>
        <p>
          <strong><%= result2[0].writer %></strong> <%= result2[0].content %>
        </p>
        <% } else { %>
        <p>등록된 댓글이 존재하지 않습니다</p>
        <% } %>
      </div>
      <form action="/comment" method="POST">
        <input name="content" />
        <input
          name="parentId"
          value="<%= result._id %>"
          style="display: none"
        />
        <button type="submit">댓글작성</button>
      </form>
    </div>
  </body>
</html>
